rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: deny all access. Update these before going to production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
